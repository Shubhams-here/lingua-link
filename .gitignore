node_modules/
dist/
*.log
.DS_Store

# Backend secrets (never commit)
backend/.env

# Frontend env files:
# .env.production and .env.development contain NO secrets (just the backend URL)
# and MUST be committed so Vercel/local builds pick them up.
# Only ignore .env.local which may contain personal overrides.
frontend/.env.local
